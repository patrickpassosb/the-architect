{
  "name": "the-architect",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev": "echo 'Run each workspace dev script in separate terminals: npm run redis:up, npm run dev:api, npm run dev:worker'",
    "dev:api": "npm run dev -w apps/api",
    "dev:worker": "npm run dev -w apps/worker",
    "lint": "npm run --workspaces lint",
    "test": "npm run --workspaces test",
    "typecheck": "npm run --workspaces typecheck",
    "redis:up": "docker compose -f infra/docker-compose.yml up -d redis",
    "redis:down": "docker compose -f infra/docker-compose.yml down"
  }
}
