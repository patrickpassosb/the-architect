# Open Questions (to unblock perfect execution)

1. Should MVP include actual microphone capture now, or text-first with voice integration as step 2?
2. Do you want us to integrate real Mistral APIs in first pass, or keep mocked provider interfaces first?
3. Preferred package manager/workspace tool: pnpm, npm workspaces, or bun?
4. Do you want Turborepo/Nx, or keep plain minimal monorepo?
5. Should we include basic observability (logs + simple metrics) in MVP?
6. For demo day, do you want a polished UI theme from day one, or functional-first UI?
7. Should artifacts be markdown only, or markdown + JSON structured outputs?
8. Target deployment for MVP demo: local only, or cloud deploy (Vercel + Railway)?
9. Should we keep the app name in code as `the-architect`, or `the-architect-ai` for potential naming fallback?
